<%- include('../partials/head') %>

<body class="min-h-screen bg-gradient-to-br from-gray-100 to-gray-200 flex">

  <!-- 🔥 LEFT SIDE -->

  <div class="hidden md:flex w-1/2 bg-blue-500 text-white flex-col justify-center items-center p-10">
    <h1 class="text-4xl font-bold mb-4">Join Us 🚀</h1>
    <p class="text-lg opacity-90 text-center max-w-md">
      Create your account and start exploring amazing properties with a seamless experience.
    </p>
  </div>

  <!-- 🔥 RIGHT SIDE -->

  <div class="w-full md:w-1/2 flex items-center justify-center px-6">

```
<div class="w-full max-w-md bg-white/90 backdrop-blur-md p-8 rounded-2xl shadow-xl">

  <!-- Title -->
  <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">
    Create Account
  </h2>

  <!-- Error -->
  <% if (error) { %>
    <p class="text-red-500 text-center mb-4"><%= error %></p>
  <% } %>

  <!-- Form -->
  <form action="/signup" method="POST" class="space-y-5">

    <!-- Name -->
    <div>
      <label class="text-sm text-gray-600">Full Name</label>
      <input
        type="text"
        name="name"
        placeholder="Enter your full name"
        class="w-full mt-1 px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-400 outline-none"
        required
      />
    </div>

    <!-- Email -->
    <div>
      <label class="text-sm text-gray-600">Email</label>
      <input
        type="email"
        name="email"
        placeholder="Enter your email"
        class="w-full mt-1 px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-400 outline-none"
        required
      />
    </div>

    <!-- Password -->
    <div>
      <label class="text-sm text-gray-600">Password</label>
      <input
        type="password"
        name="password"
        placeholder="Create a password"
        class="w-full mt-1 px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-400 outline-none"
        required
      />
    </div>

    <!-- Button -->
    <button
      type="submit"
      class="w-full bg-blue-500 text-white py-3 rounded-lg font-semibold hover:bg-blue-600 transition shadow-md"
    >
      Create Account
    </button>

  </form>

  <!-- Login Link -->
  <p class="text-center text-gray-600 mt-5">
    Already have an account?
    <a href="/login" class="text-blue-500 font-medium hover:underline">
      Sign In
    </a>
  </p>

  <!-- Back -->
  <div class="mt-6 text-center">
    <a href="/" class="text-gray-500 hover:text-black transition text-sm">
      ← Back to Home
    </a>
  </div>

</div>
```

  </div>

</body>
</html>
