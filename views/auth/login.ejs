<%- include('../partials/head') %>

<body class="min-h-screen bg-gradient-to-br from-gray-100 to-gray-200 flex">

  <!-- 🔥 LEFT SIDE (Brand / Info) -->

  <div class="hidden md:flex w-1/2 bg-red-500 text-white flex-col justify-center items-center p-10">
    <h1 class="text-4xl font-bold mb-4">Welcome Back</h1>
    <p class="text-lg opacity-90 text-center max-w-md">
      Manage your properties, explore listings, and enjoy a seamless rental experience.
    </p>
  </div>

  <!-- 🔥 RIGHT SIDE (Login Form) -->

  <div class="w-full md:w-1/2 flex items-center justify-center px-6">

```
<div class="w-full max-w-md bg-white/90 backdrop-blur-md p-8 rounded-2xl shadow-xl">

  <!-- Title -->
  <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">
    Sign In
  </h2>

  <!-- Error -->
  <% if (error) { %>
    <p class="text-red-500 text-center mb-4"><%= error %></p>
  <% } %>

  <!-- Form -->
  <form action="/login" method="POST" class="space-y-5">

    <!-- Email -->
    <div>
      <label class="text-sm text-gray-600">Email</label>
      <input
        type="email"
        name="email"
        placeholder="Enter your email"
        class="w-full mt-1 px-4 py-3 border rounded-lg focus:ring-2 focus:ring-red-400 outline-none"
        required
      />
    </div>

    <!-- Password -->
    <div>
      <label class="text-sm text-gray-600">Password</label>
      <input
        type="password"
        name="password"
        placeholder="Enter your password"
        class="w-full mt-1 px-4 py-3 border rounded-lg focus:ring-2 focus:ring-red-400 outline-none"
        required
      />
    </div>

    <!-- Button -->
    <button
      type="submit"
      class="w-full bg-red-500 text-white py-3 rounded-lg font-semibold hover:bg-red-600 transition shadow-md"
    >
      Sign In
    </button>

  </form>

  <!-- Signup -->
  <p class="text-center text-gray-600 mt-5">
    Don’t have an account?
    <a href="/signup" class="text-red-500 font-medium hover:underline">
      Create Account
    </a>
  </p>

  <!-- Back -->
  <div class="mt-6 text-center">
    <a href="/" 
       class="text-gray-500 hover:text-black transition text-sm">
      ← Back to Home
    </a>
  </div>

</div>
```

  </div>

</body>
</html>
