<nav class="flex justify-between items-center px-8 py-4 bg-white/80 backdrop-blur-md shadow-sm sticky top-0 z-50">

  <!-- LOGO -->
  <div class="flex items-center gap-2">
    <h1 class="text-2xl font-bold text-red-500 cursor-pointer">
      Rentify
    </h1>
  </div>

  <!-- RIGHT SIDE -->
  <div class="flex items-center gap-4">

    <% if (!isLoggedIn) { %>

      <!-- HOST BUTTON -->
      <a href="/register" class="hidden md:block text-gray-700 hover:text-red-500 font-medium">
        Rentify your home
      </a>

      <!-- LOGIN -->
      <a href="/login" 
        class="bg-red-500 text-white px-4 py-2 rounded-full hover:bg-red-600 transition">
        Login
      </a>

    <% } else { %>

      <!-- ADD HOME -->
      <a href="/register" 
        class="bg-gradient-to-r from-red-500 to-pink-500 text-white px-5 py-2 rounded-full hover:opacity-90 transition">
        + Add Home
      </a>

      <!-- LOGOUT -->
      <a href="/logout" 
        class="bg-gray-900 text-white px-4 py-2 rounded-full hover:bg-black transition">
        Logout
      </a>

    <% } %>

  </div>

</nav>